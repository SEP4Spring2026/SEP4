[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-ra"

[tool.coverage.run]
source = ["main"]
omit = [
    ".venv/*",
    "tests/*",
    "dataGenerator.py",
]

[tool.coverage.report]
show_missing = true
fail_under = 70

[tool.ruff.lint]
select = ["E", "F", "W"]
